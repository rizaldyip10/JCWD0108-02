import { Box } from "@chakra-ui/react"
import { Counter } from "../components/counter"

export const Detailpage = () => {
    return(
        <Box py={20}>
            
        <Counter/>
        </Box>
    )
}